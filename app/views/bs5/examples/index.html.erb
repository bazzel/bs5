  <div class="row">
    <div class="col">
      <div class="sticky-top">
        <ul class="nav flex-column">
        <li>Accordion</li>
        <li>Alert</li>
        <li>Badge</li>
        <li>Close button</li>
        <li>Breadcrumb</li>
        <li>Buttons</li>
        <li>Tooltips</li>
        </ul>
      </div>
    </div>
    <div class="col-10">
      <h1>Bootstrap Examples</h1>
      <%= render 'accordion' %>
      <%= render 'alert' %>
      <%= render 'badge' %>
      <%= render 'close_button' %>
      <%= render 'breadcrumb' %>
      <%= render 'buttons' %>
      <%= render 'tooltips' %>
    </div>
  </div>