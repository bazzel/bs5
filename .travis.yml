language: ruby
rvm:
  - 2.6.6
install:
  - bundle install --jobs 3 --retry 3
script:
  - bin/rails ci
cache:
  bundler: true
  directories:
    - node_modules
  yarn: true
branches:
  only:
    - main
    - develop
    - /^feature\/.+$/
